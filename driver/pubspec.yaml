name: driver
description: "Driver app for Ghumo"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  intl_phone_field: ^3.1.0
  pinput: ^3.0.1

  firebase_core: ^2.24.2
  firebase_messaging: ^14.7.16
  cloud_firestore: ^4.15.5
  firebase_auth: ^4.5.0
  firebase_storage: ^11.6.6

  flutter_local_notifications: ^16.3.2
  bloc: ^8.1.3
  equatable: ^2.0.0
  flutter_bloc: ^8.1.4
  http:
  file_picker: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true