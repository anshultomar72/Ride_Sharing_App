syntax = "proto3";

package common_packages.lib.src.protos;

message User {
    string id = 1;
    string name = 2;
    string email = 3;
    repeated string phone_numbers = 4;
    UserType type = 5;
}

enum UserType {
    DRIVER = 0;
    RIDER = 1;
}
